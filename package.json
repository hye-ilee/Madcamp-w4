{
  "name": "jjolab",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "serve": "nodemon backend/index.js",
    "start": "yarn --cwd backend install && node backend/index.js",
    "build": "yarn --cwd frontend install && yarn --cwd frontend build"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
